<Window x:Class="Brusnika.Views.Windows.OrderDishesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Brusnika.Views.Windows"
        mc:Ignorable="d"
        Title="OrderDishesWindow" Height="600" Width="850"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Новый заказ" FontSize="24" FontWeight="Bold" Margin="10"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height=".2*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <!-- Каталог товаров -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Каталог блюд" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ListBox ItemsSource="{Binding Dishes}" 
                 SelectedItem="{Binding SelectedDish, Mode=TwoWay}"
                 DisplayMemberPath="Name"
                 Height="300" Width="370"/>
                    <Button Content="Добавить в заказ" Width="250"
                Click="AddProduct_Click"
                Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Товары в заказе -->
                <StackPanel >
                    <TextBlock Text="Блюда в заказе" FontWeight="Bold" Margin="0,0,0,5" />
                    <ListBox ItemsSource="{Binding SelectedDishes}"
                 SelectedItem="{Binding SelectedFromOrder, Mode=TwoWay}"
                 Height="300">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Dish.Name}" Width="300" TextWrapping="Wrap"/>
                                    <Button Content="−" Width="35" Height="35" Margin="5,0,0,0"
                                Click="DecreaseQuantity_Click"/>
                                    <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center" Margin="5,0"/>
                                    <Button Content="+" Width="35" Height="35"
                                Click="IncreaseQuantity_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Content="Удалить из заказа" Width="250"
                Click="RemoveProduct_Click"
                Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Оформление заказа -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <Button Content="Оформить заказ"
            Click="PlaceOrder_Click"
            Margin="0,0,0,10"/>
            </StackPanel>

        </Grid>
    </Grid>
</Window>
